# إصلاح: لماذا كان الـ AI Agent يرفض إعطاء بياناتك الخاصة — والحل

التاريخ: 2026-06-13

## المشكلة (من سكرين شوت رامي)

عند سؤال البوت على Telegram: «أعطني تقريراً عن آخر حالة أو تحديثات الـ Local AI Brain لـ DRFONE»،
ردّ البوت باعتذار عن «الخصوصية» وأن البيانات «غير متاحة علنياً». هذا الرد كان **هلوسة**،
وليس بسبب حماية فعلية للبيانات.

## السبب الجذري

تتبّع المسار داخل n8n:
`DRFONE_AI_AGENT_ROOM_V3.24_STAGING` → عقدة `AI Brain Context Builder` → `Call AI Brain (Brain Router)` → `DRFONE_BRAIN_ROUTER`.

1. سؤالك سار عبر مسار الدردشة الحرّة (`/ai_chat`). في هذا المسار كان النظام يرسل **نص سؤالك فقط بدون أي بيانات/سياق** إلى الموديل.
2. الدماغ الرئيسي (PC GPU عبر `ollama.drfone.eu`) كان غير متاح، فوقع الطلب على البديل الأضعف `qwen2.5:3b` على VPS CPU (لذلك ظهر الوسم `vps-cpu`).
3. موديل صغير + صفر بيانات = اختراع رد اعتذار خصوصية بدا منطقياً لكنه غير صحيح.

الخلاصة: **الـ agent لا يعرف إلا ما تُغذّيه إياه كسياق، ولم يكن يُغذّى بأي شيء في الأسئلة الحرّة.**

## ما تم تعديله (workflow: `pzYcFv1ETTwKzzTw`)

### 1) عقدة `AI Brain Context Builder` — حقن السياق + منع الهلوسة
- الآن تُرفق المصادر المتاحة (مجلدات Drive 01/02/04 + سياق العمل أونلاين) مع **كل** سؤال،
  بما فيها الدردشة الحرّة `/ai_chat` (كانت سابقاً بلا أي سياق).
- نظام تعليمات (system prompt) صارم: أجب فقط من السياق؛ وإن لم تتوفر الإجابة قل بصراحة
  «ما عندي بيانات عن هذا في مصادري الحالية» واقترح `/status` أو `/review`؛
  **ويُمنع منعاً باتاً اختراع أعذار خصوصية/قانونية أو ادّعاء أن البيانات محمية**.
- كشف أسئلة حالة النظام/الدماغ وتوجيه المستخدم لاستخدام `/status`.

### 2) عقدة `Status Reply` — فحص حيّ حقيقي لحالة الدماغ
عند كتابة `/status` يقوم البوت الآن بفحص حيّ متوازٍ للمسارات الثلاثة ويعرض حالتها الفعلية:
- 🟢/🔴 الدماغ الرئيسي PC GPU — `https://ollama.drfone.eu/api/tags`
- 🟢/🔴 الدماغ الاحتياطي VPS CPU — `http://ollama:11434/api/tags` (مع عدد الموديلات)
- 🟢/🔴 واجهة الدردشة — `https://chat.drfone.eu/`

التنفيذ عبر `this.helpers.httpRequest` داخل العقدة نفسها — بدون أي تغيير على ربط الـ workflow.

## ما لم يُنفَّذ ولماذا (RAG / البحث الدلالي)

البحث الدلالي عبر Qdrant **غير قابل للتفعيل حالياً**: لا يوجد أي credential لـ Qdrant،
ولا workflow لفهرسة (ingestion)، ولا أي collection مفهرسة. تفعيله الآن سيعيد نتائج فارغة.

### المتطلبات لبناء RAG لاحقاً
1. تشغيل Qdrant (self-hosted على نفس الـ VPS) وإضافة credential له في n8n.
2. بناء workflow فهرسة: يقرأ وثائقك (Drive/ملاحظات) → embeddings → تخزين في Qdrant.
3. ربط عقدة بحث Qdrant في الـ agent تحقن النتائج في السياق قبل الموديل.

بديل عملي جاهز الآن لبيانات المهام: جدول البيانات `DRFONE_AI_OPERATIONS_ROOM_TASKS`
(موجود فعلاً، 24 عموداً) يمكن ربطه كمصدر منظَّم في خطوة لاحقة.

## كيفية التحقق
1. أرسل `/status` على Telegram → يجب أن تظهر حالة حيّة للمسارات الثلاثة.
2. أعد طرح السؤال الأصلي → بدل اعتذار الخصوصية، يجب أن يجيب من السياق أو يقول بصراحة
   «ما عندي بيانات… استخدم /status».
